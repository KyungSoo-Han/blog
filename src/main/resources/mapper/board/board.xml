<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.hankyungsoo.blog.board.mapper.BoardMapper">

    <insert id="boardSave" parameterType="kr.hankyungsoo.blog.board.dto.BoardDto" useGeneratedKeys="true" keyColumn="id">
        insert into t_board(
                            title, content, view_cnt, created_id, created_dt
        )values (
                 #{title}, #{content}, #{viewCnt}, #{createdId}, #{createdDt}
                        )
    </insert>

</mapper>

    